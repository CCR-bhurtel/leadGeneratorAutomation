const axios = require('axios');

// gets webhook signal whenever something is changed in ninox and sends response to respond.io
const ninoxWebHookController = (req, res) => {
    const payload = req.body;
};

module.exports = ninoxWebHookController;
